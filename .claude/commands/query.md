Answer a question using the wiki knowledge base. Follow the query operation defined in CLAUDE.md:

1. Read `index.md` to find relevant pages
2. Read the relevant pages
3. Synthesize an answer with citations to specific wiki pages
4. If the answer is substantial and reusable, file it as a new page in `pages/explorations/`
5. Append a query entry to `log.md`

Cite wiki pages by relative path. If the wiki doesn't have enough information to answer fully, say what's known and identify the knowledge gap — suggest what sources would need to be ingested to fill it.
